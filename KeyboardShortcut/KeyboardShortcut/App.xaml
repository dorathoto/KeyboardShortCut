<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="KeyboardShortcut.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:control="using:Microsoft.UI.Xaml.Controls"
    xmlns:local="using:KeyboardShortcut"
     xmlns:h="using:H.NotifyIcon">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />

                <ResourceDictionary>
                    <CornerRadius x:Key="OverlayCornerRadius">0</CornerRadius>
                    <CornerRadius x:Key="ControlCornerRadius">0</CornerRadius>
                </ResourceDictionary>
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <!-- Other app resources here -->

            <local:PathToIconConverter x:Key="PathToIconConverter" />

            <h:TaskbarIcon
                x:Name="TrayIcon"
                IconSource="/Assets/store.ico"
                ToolTipText="Keyboard Shortcut Bar">
				<h:TaskbarIcon.ContextFlyout>
                    <MenuFlyout>
                        <MenuFlyoutItem Text="Mostrar Atalhos" x:Name="ShowMenuItem"/>
                        <MenuFlyoutSeparator/>
                        <MenuFlyoutItem Text="Sair" x:Name="ExitMenuItem"/>
                    </MenuFlyout>
                </h:TaskbarIcon.ContextFlyout>
            </h:TaskbarIcon>
        </ResourceDictionary>

    </Application.Resources>
</Application>
